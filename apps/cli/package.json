{
  "name": "@pi-starter/cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "echo 'cli build pending packaging setup'",
    "test": "tsx --test test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pi-starter/ai": "1.0.0",
    "@pi-starter/contracts": "1.0.0",
    "rabbitbrain": "1.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
